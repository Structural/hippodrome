{
  "name": "hippodrome",
  "version": "0.1.7",
  "description": "This package.json is just to build the source files.  The one that actually gets published to npm is at npm/package.json.",
  "scripts": {
    "test": "jasmine-node --coffee --verbose test",
    "build": "gulp build"
  },
  "engines": {
    "node": "0.10.x"
  },
  "dependencies": {
    "lodash": "^2.4.1"
  },
  "devDependencies": {
    "gulp": "^3.8.9",
    "gulp-coffee": "^2.2.0",
    "gulp-concat": "^2.4.1",
    "gulp-insert": "^0.4.0",
    "gulp-rename": "^1.2.0",
    "gulp-shell": "^0.2.10",
    "gulp-uglify": "^1.0.1"
  }
}
